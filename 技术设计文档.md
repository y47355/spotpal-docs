# 搭趣 SpotPal · 技术设计文档

> **版本** V1.0 · 2026-09-09 · 状态：评审稿
>
> **配套产出**
> - 高保真原型（6 屏）：Ardot 文件 `723889208012364` · Page 1
> - 软件架构图（3 幅）：Ardot 文件 `723889208012364` · Page 2「软件架构设计」
> - 操控玩法设计（4 帧手势分解 + 流程）：Ardot 文件 `724002923294341`「上推托付 Push & Trust」
> - 可交互演示：`spotpal-demo/搭趣SpotPal-演示版.html`（单文件，56KB，零依赖）
> - 详细设计：`详细设计-服务端.md`（SQLite 零依赖版）· `详细设计-客户端.md`（Android）
>
> **⚠️ 初版实施范围注记**（2026-09-10）：本文档描述产品终态。初版实施以详细设计为准——支付降级为纯记账（无微信 SDK，费用线下自理，接入点隔离在 `pay.Ledger` 接口后）；定位采用高德 SDK；数据层 SQLite 单文件起步。演进触发器见详细设计第 9 章。

---

## 1. 产品概述

### 1.1 一句话定位

基于个人兴趣与实时位置、以 **AI 代协商**为核心的轻社交产品——解决「临时想做什么，都有人一起」的问题。

### 1.2 痛点与解法

| 痛点 | 现状 | 搭趣解法 |
|---|---|---|
| 临时起意找不到人 | 微信群里喊一圈，等半天没回音 | LBS + 兴趣匹配，分钟级给出候选搭子 |
| 约时间/谈费用尴尬 | 反复拉扯「你几点行」「AA 还是请」 | AI 协商引擎代谈时间、项目、费用分摊 |
| 陌生搭子无信任基础 | 怕放鸽子、怕体验差 | 信用分 + 互评体系，履约有据可查 |
| 约局工具太重 | 组局 App 表单长、门槛高 | 上推托付手势：1 个动作完成「选人+托付」 |

### 1.3 核心差异化

**AI 协商引擎**——用户只表达意图（上推候选池），AI 负责与候选搭子完成多轮协商（时间折中/项目确认/费用方式/角色分配），双方无需同时在线。协商结果以方案卡形式推送，一键确认或摇回去重谈。

---

## 2. 信息架构与交互协议

### 2.1 页面结构（6 屏）

```
发现（首页） ── 搭子主页 ── AI 协商 ── 搭局确认 ── 我的搭局 ── 我的
     │            │                                │
     └── 推荐卡流   └── 邀请搭局 CTA                 └── 历史与信用
```

| 屏幕 | 职责 | 关键模块 |
|---|---|---|
| 发现 | 找人 | 定位栏、兴趣标签横滑、AI 推荐卡（匹配度/AI 理由/距离/活跃度）、搭局条 |
| 搭子主页 | 识人 | 大头像、数据栏、暗色 AI 推荐理由卡、共同兴趣、费用偏好、邀请搭局 CTA |
| AI 协商 | 成局核心 | 协商进度条、聊天气泡流（方案卡/费用选项/暗色汇总卡） |
| 搭局确认 | 锁定 | 方案卡、费用明细表（AA/轮流/一人请）、参与人状态、确认锁单 |
| 我的搭局 | 履约 | 进行中卡（倒计时/头像堆叠）、待确认、历史记录（含评分） |
| 我的 | 资产 | 个人卡、暗色信用分卡（96 分）、统计三卡、兴趣标签管理 |

### 2.2 操控玩法：上推托付 Push & Trust（v2 定稿）

> v1 的「左右摇摆选时间」因手机上操作精度不足被砍掉，时间选择收敛到长按面板内的小滑块。

| 手势 | 触发条件 | 行为 | 状态流转 |
|---|---|---|---|
| **静置浏览** | — | 卡片默认带「今晚 20:00」搭局条，直接浏览 | `IDLE` |
| **长按** | 按住 600ms | 弹出微调面板：搭局条小滑块选时间（左右拖，带刻度）、活动项目、费用方式、我的角色 | `ADJUSTING` |
| **上推** | 垂直上滑过阈值（80px） | 卡片飞入顶部候选池，「先收着，让 AI 去谈」；候选池人数实时可见 | `POOLED` |
| **释放** | 松手且候选池非空 | 意图锁定，生成暗色确认层「AI 开始协商」；AI 按匹配度逐个试探候选 | `NEGOTIATING` |

**交互规则**

- 候选池内的人可随时移除（长按候选池条目），无社交成本；
- 协商任何环节超时未确认（48h），卡片自动回流推荐池；
- 全部手势单手可完成，位于拇指热区；
- 上滑未过阈值即松手 = 取消，卡片弹回原位（防误触）。

### 2.3 AI 协商链路（8 步）

```
① 用户上推候选池（兴趣+位置+时间窗+角色偏好）
② 业务服务 LBS 检索 + 匹配引擎召回候选
③ 返回推荐列表（匹配度/AI 理由/距离/活跃度）
④ 用户释放，委托 AI 发起协商
⑤ AI 多轮协商：解析双方时间偏好/项目/费用意向 → 生成折中方案卡
⑥ WebSocket 推送方案卡，双方各自查看/微调
⑦ 双方确认 → 搭局创建，进入「待履约」
⑧ 活动完成 → 费用分摊结算（微信支付分账）→ 互评 → 信用分更新
```

**协商引擎护栏**

- 输出强制 JSON Schema（时间/项目/费用三字段），杜绝 LLM 自由发挥；
- 外层状态机控制轮次与超时：≤5 轮、48h 失效；
- 可回滚：任何一轮可回到上一个双方共识点；
- 全程 Kafka 留痕，可回溯审计。

---

## 3. 系统架构

### 3.1 六层架构总览

```
┌─────────────────────────────────────────────────────────┐
│ L1 客户端层  iOS App (SwiftUI) · Android (Compose)        │
│             微信小程序（获客入口）· 运营管理后台 (Web)      │
├─────────────────────────────────────────────────────────┤
│ L2 接入层    API 网关（鉴权/限流/路由）                     │
│             WebSocket 长连接（协商/IM 实时推送）· CDN        │
├─────────────────────────────────────────────────────────┤
│ L3 业务服务层 用户（画像/兴趣标签）· 搭局（发起/确认/状态机）  │
│             支付（费用分摊/结算）· IM · 信用评价 · 安全风控   │
├─────────────────────────────────────────────────────────┤
│ L4 AI 能力层 ★差异化核心                                   │
│             AI 协商引擎（核心）· 兴趣+LBS 匹配引擎           │
│             LLM 对话服务（GPT/混元）· Embedding · 推荐排序  │
├─────────────────────────────────────────────────────────┤
│ L5 数据层    MySQL 业务主库 · Redis 缓存/会话               │
│             ES 兴趣搜索 · Milvus 向量库 · OSS · Kafka       │
├─────────────────────────────────────────────────────────┤
│ L6 基础设施  K8s 容器编排 · Prometheus+Grafana 监控        │
│             ELK 日志链路追踪 · CI/CD · 多可用区容灾          │
└─────────────────────────────────────────────────────────┘
```

### 3.2 服务拆分与职责

| 服务 | 职责 | 核心依赖 |
|---|---|---|
| user-svc | 注册登录、画像、兴趣标签、信用分 | MySQL、Redis |
| match-svc | 三路召回（ES 标签/Milvus 向量/Redis GEO）、融合排序 | ES、Milvus、Redis |
| squad-svc | 搭局状态机（见 4.1）、候选池、生命周期事件 | MySQL、Kafka |
| negotiate-svc | AI 协商编排、LLM 调用、方案卡生成 | LLM、Kafka、Redis |
| im-svc | 会话消息、系统通知 | WebSocket 网关、Kafka |
| pay-svc | 结算指令、微信分账、退款 | 微信支付 API |
| credit-svc | 互评采集、信用分计算 | MySQL、Kafka |
| risk-svc | 设备指纹、行为异常、内容审核 | 规则引擎、Kafka |

### 3.3 关键技术决策

| # | 决策点 | 方案 | 理由 |
|---|---|---|---|
| 1 | 匹配召回 | 标签（ES 倒排）+ 行为向量（Milvus ANN）+ 地理围栏（Redis GEO）三路召回，加权融合 | 冷启动阶段标签+LBS 开箱即用；向量路随数据积累增强；三路互为兜底 |
| 2 | AI 协商 | LLM 结构化输出（JSON Schema 约束）+ 状态机护栏（≤5 轮、48h 超时、可回滚） | LLM 出创意，状态机保确定性 |
| 3 | 实时通讯 | 自建 WebSocket 网关，协商推送/IM/状态变更统一走长连 | 协商过程强定制，第三方 IM SDK 难以深度嵌入状态机 |
| 4 | 位置隐私 | 商圈级模糊化（1km 网格）、匹配用后即焚、可一键关闭位置 | 《个保法》最小必要；位置是核心数据也是最大合规风险 |
| 5 | 资金安全 | 微信支付分账 API，平台零资金沉淀 | 规避二清风险；信用体系约束履约而非资金冻结 |
| 6 | 冷启动 | 小程序先行 + 单城市（深圳）垂类打透，再复制 | 双边市场密度优先于广度 |

---

## 4. 核心模块详细设计

### 4.1 搭局状态机（squad-svc）

```
IDLE ──上推──▶ POOLED ──释放──▶ NEGOTIATING ──双方确认──▶ CONFIRMED
                                     │                      │
                                     │ 超时48h/5轮失败        │ 活动完成
                                     ▼                      ▼
                               EXPIRED（回流推荐池）      SETTLED ──▶ REVIEWED
                                                        （结算+互评）
```

状态迁移表：

| 迁移 | 触发 | 动作 |
|---|---|---|
| IDLE→POOLED | 用户上推 | 写候选池，投递 `pool.joined` 事件 |
| POOLED→NEGOTIATING | 释放/AI 试探开始 | 创建协商会话，锁定意图快照 |
| NEGOTIATING→CONFIRMED | 双方确认方案卡 | 生成搭局单，写日历提醒 |
| NEGOTIATING→EXPIRED | 48h 超时或 5 轮失败 | 卡片回流，通知双方 |
| CONFIRMED→SETTLED | 活动完成（任一方打卡或时间过窗+24h） | 生成结算指令 |
| SETTLED→REVIEWED | 双方互评提交（或 72h 默认好评） | 更新信用分 |

### 4.2 匹配引擎（match-svc）

**三路召回 → 融合排序**

```
score = 0.4·tag_score + 0.35·vec_score + 0.25·geo_score
```

| 召回路 | 数据源 | 说明 |
|---|---|---|
| tag_score | ES 倒排（兴趣标签） | Jaccard 相似度，冷启动主力 |
| vec_score | Milvus（用户行为 Embedding） | ANN TopK，随行为数据增强 |
| geo_score | Redis GEO（1km 网格） | 距离衰减函数 exp(-d/2km) |

排序后叠加活跃度权重（7 日内活跃 ×1.5）与风控过滤（黑名单/设备异常直接剔除）。推荐卡上的「AI 理由」由 LLM 基于匹配维度生成一句话，缓存 24h。

### 4.3 AI 协商引擎（negotiate-svc）

**会话结构**

```json
{
  "negotiation_id": "ngt_8f3a",
  "initiator": {"user_id": "u_1024", "time_pref": "今晚20:00±1.5h", "role": "可发起可加入"},
  "candidates": [{"user_id": "u_2048", "match_score": 0.87}],
  "rounds": [
    {"round": 1, "proposal": {"time": "20:00", "activity": "羽毛球", "cost": "AA"}, "status": "countered"}
  ],
  "guardrails": {"max_rounds": 5, "ttl_hours": 48}
}
```

**协商流程**

1. 从候选池取匹配度 Top1，拉取双方意图快照（时间窗/项目/费用偏好/角色）；
2. LLM 生成首轮方案（JSON Schema 约束输出）；
3. 推送给双方，收集微调（WebSocket，异步，无需同时在线）；
4. 若有分歧，LLM 基于双方最近一轮偏好生成折中方案（最多 5 轮）；
5. 达成一致 → 方案卡确认 → 搭局创建；
6. 失败/超时 → 换下一个候选（回到步骤 1），全部失败则回流。

**成本控制**：意图快照做 Redis 缓存；方案卡生成结果幂等存储，重试不重复计费；LLM 优先用混元（成本 1/5），复杂协商才路由 GPT。

### 4.4 实时通讯（WebSocket 网关）

- 连接管理：单用户多端在线（App+小程序），消息按端推送，ACK 去重；
- 消息类型：协商事件（proposal/confirm/expire）、IM 聊天、系统通知；
- 可靠性：消息经 Kafka 投递，网关消费后推送；断线重连后按 seq 补拉；
- 容量：单网关节点 5w 长连，K8s HPA 按 CPU 60% 扩容。

---

## 5. 数据模型（核心表）

```sql
-- 用户与画像
user(id, phone, nickname, avatar_url, city, credit_score, created_at)
user_interest(user_id, tag, weight, updated_at)          -- 兴趣标签
user_pref(user_id, cost_mode, time_slots, role_pref)      -- 费用/时间/角色偏好

-- 搭局
squad(id, initiator_id, status, activity, time_window,     -- 状态机主表
      cost_mode, settle_status, created_at, expired_at)
squad_member(squad_id, user_id, role, confirm_status, joined_at)
candidate_pool(user_id, candidate_id, intent_snapshot,     -- 候选池（意图快照 JSON）
               status, created_at)

-- 协商
negotiation(id, squad_id, current_candidate, round_count, status, created_at)
negotiation_round(id, negotiation_id, round_no, proposal_json, both_feedback, created_at)

-- 信用与结算
review(id, squad_id, reviewer, reviewee, rating, tags, comment, created_at)
settle_order(id, squad_id, mode, amount, wx_order_id, status, created_at)
```

**位置数据不落库**：实时坐标仅 Redis GEO（TTL 15min），匹配用后即焚，符合最小必要原则。

---

## 6. API 设计（核心端点）

```
POST /v1/pool/push              上推候选（body: candidate_id, intent）
GET  /v1/pool                    候选池列表（含 AI 试探进度）
DELETE /v1/pool/{candidate_id}   移除候选
POST /v1/negotiation/release     释放 → AI 开始协商
GET  /v1/negotiation/{id}        协商会话详情（轮次/方案卡）
POST /v1/negotiation/{id}/feedback  微调反馈（双方）
POST /v1/squad/{id}/confirm      确认成局
POST /v1/squad/{id}/checkin      活动打卡（触发结算）
GET  /v1/feed/recommend          推荐卡流（分页，含 AI 理由）
WS   /v1/ws                      长连接（协商事件/IM/通知）
```

约定：REST + JSON，鉴权 Bearer Token；写操作幂等键 `Idempotency-Key`；分页 cursor 式。

---

## 7. 非功能设计

### 7.1 安全与合规

- **位置**：商圈级展示、匹配即焚、可关闭（关闭后按城市维度匹配）；
- **敏感数据**：位置/通讯录授权单独同意页（个保法要求）；
- **内容**：LLM 输出过敏感词过滤 + 人工抽检；用户 UGC 接入文本审核 API；
- **资金**：不碰池子，分账走微信，结算单双写对账。

### 7.2 性能与容量（V1 目标：深圳单城 5w DAU）

| 指标 | 目标 |
|---|---|
| 推荐流首屏 | P95 < 800ms（召回并行，缓存 AI 理由） |
| 协商方案卡生成 | P95 < 5s（LLM 流式） |
| WebSocket 推送 | P95 < 500ms |
| 匹配召回 QPS | 峰值 500（三路并行，超时降级单路） |
| 数据规模 | 用户 50w、搭局 100w/年、协商轮次 500w/年 |

### 7.3 可观测性

- 全链路 traceId（网关注入，Kafka 消息携带）；
- 协商漏斗看板：上推→释放→协商→成局→结算 五级转化率；
- LLM 调用监控：tokens/成本/延迟/Schema 违规率（>1% 告警）。

---

## 8. 里程碑

| 阶段 | 周期 | 交付 |
|---|---|---|
| M1 骨架 | 4 周 | 网关/用户/搭局状态机跑通，推荐流（仅标签+GEO 召回） |
| M2 核心玩法 | 4 周 | 上推托付手势、候选池、WebSocket 推送、AI 协商 MVP（混元） |
| M3 闭环 | 3 周 | 微信分账、互评信用分、风控接入、可观测看板 |
| M4 打磨 | 3 周 | 性能优化、合规审查、灰度（深圳 1000 种子用户） |

---

## 9. 风险与开放问题

| 风险 | 影响 | 缓解 |
|---|---|---|
| LLM 协商质量不稳定 | 成局率低 | Schema 约束 + 5 轮护栏 + 方案卡人工抽样评审 |
| 双边冷启动密度不足 | 匹配空转 | 小程序裂变 + 垂类运营（羽毛球/剧本杀单品类打透） |
| 手势学习成本 | 上推误触率 | 阈值 80px + 松手回弹；首次进入 3 帧引导浮层 |
| 位置隐私舆情 | 上架风险 | 默认商圈级 + 关闭入口前置 + 隐私白皮书 |

**开放问题**（待评审拍板）：候选池上限人数（建议 5）；协商并发上限（建议同时 3 个会话）；信用分冷启动初始值与衰减规则。

---

*本文档由设计稿反向整理生成，架构图/原型/玩法图均可回溯到对应 Ardot 画布页。*
