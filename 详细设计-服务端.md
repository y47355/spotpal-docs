# 搭趣 SpotPal · 服务端详细设计（SQLite 零依赖版）

> **仓库** `spotpal-server` · 单二进制 + 单数据文件 · 模块化单体
> **定位** 初版零基础设施负担，一条命令全链路可跑；量级增长按演进路径逐级升配
> **技术栈** Go 1.22 · SQLite（modernc.org/sqlite 纯 Go 驱动，无 CGO）· 内嵌 HTTP + WebSocket
> **上游文档** 《技术设计文档.md》V1.0（2026-09-09）·《详细设计-客户端.md》（契约不变，零改动）

---

## 0. 设计立场

### 0.1 为什么是 SQLite，不是 Redis

用户问「PG 也重了，能否换 SQLite 或 Redis」。结论：**SQLite，不选 Redis**。

| 维度 | SQLite | Redis（当主库用） |
|---|---|---|
| 数据模型 | 关系型：标签交集 JOIN、状态机唯一约束、JSON 函数齐全 | KV/内存结构：关系查询是弱项 |
| 持久化 | 单文件落盘，ACID 事务，崩溃安全 | RDB/AOF 是附加配置，默认内存态 |
| 运维 | **零运维**：一个数据文件，备份 = `cp` | 需要守护进程、内存监控、持久化调优 |
| 与业务匹配度 | 候选池/搭局/协商全是关系数据 | 更适合当缓存/队列，不适合当主存储 |

**Redis 的正确位置**是缓存和延迟队列——那正是上一轮轻量化砍掉的东西。让 Redis 当唯一主库，等于用它的短处替换 PG 的长处。SQLite 才是「初版不需要大数据量高并发」的直接答案：

- 单进程单写者（我们的部署形态本来就没有多副本）——恰好是 SQLite 并发模型的设计场景；
- 一个 `spotpal` 可执行文件 + 一个 `spotpal.db` 数据文件，**没有容器、没有端口、没有健康检查**；
- 十万行级数据、单机低并发——SQLite 的舒适区（官方口径可支持到 TB 级，我们离它十万八千里）。

### 0.2 一句话架构

```
Android / iOS / 小程序 ──HTTP :8080 / WS :8081──▶ spotpal-server（Go 单进程）
                                                      │
                                                      ▼
                                                 spotpal.db（SQLite 单文件）
```

---

## 1. 架构总览

### 1.1 目录结构

```
spotpal-server/
├── cmd/server/main.go          # 唯一入口
├── internal/
│   ├── api/                     # HTTP 路由、中间件（鉴权/限流/trace）
│   ├── ws/                      # WebSocket 网关（连接管理/推送/ACK）
│   ├── module/
│   │   ├── user/  match/  squad/  negotiate/  im/  pay/  credit/  risk/
│   │   └── …                    # 8 模块与前一版完全一致（pay 初版为纯记账，见 §2.5/§3.7）
│   ├── platform/                # 外部依赖接口：llm / sms（初版仅此两项，均可 mock）
│   ├── pkg/                     # log / config / 缓存 LRU
│   ├── bus/                     # 进程内事件总线 + outbox（SQLite 表）
│   └── timeout/                 # 超时驱动（SQLite timeouts 表）
├── migrations/                  # SQL 文件（SQLite 方言）
└── Makefile                     # make dev 直接本机跑，无容器
```

### 1.2 模块间通信规则（不变）

| 交互类型 | 方式 |
|---|---|
| 同步查询 | 进程内接口（interface 定义在消费方模块） |
| 状态变更广播 | 进程内事件总线（8 主题契约不变，见 §5） |
| 实时推送 | 模块 → ws 网关 → 客户端 |

强制约束不变：模块间禁止 import 对方内部包，只依赖 interface + 事件契约。

---

## 2. 部署：零基础设施

### 2.1 启动方式（没有 compose 文件）

```bash
make dev            # 本机直接跑：go run ./cmd/server + air 热重载
./spotpal-server    # 构建产物即部署：单二进制，双击/一条命令
make seed           # 演示数据：3 用户 / 5 搭局 / 2 轮协商记录
```

数据文件默认 `./data/spotpal.db`（`--data` 参数可改）。**备份 = 停服后 `cp`**，或运行时 `VACUUM INTO 'backup.db'`（在线热备）。

### 2.2 SQLite 关键配置（WAL 模式）

```go
db, _ := sql.Open("sqlite", "file:data/spotpal.db?_pragma=journal_mode(WAL)&_pragma=busy_timeout(5000)&_pragma=foreign_keys(ON)&_pragma=synchronous(NORMAL)")
db.SetMaxOpenConns(1)   // 写连接池=1（单写者模型，见 §2.3）
```

- **WAL 模式**：读写不互斥，读连接可多开；
- **busy_timeout 5s**：写锁竞争时排队等待而非报错；
- **synchronous=NORMAL**：WAL 下安全且快的标准搭配；
- **纯 Go 驱动**（modernc.org/sqlite）：无 CGO，交叉编译 `GOOS=linux GOARCH=arm64 go build` 一条命令，云端部署不需要任何目标机工具链。

### 2.3 并发模型：单写多读

- **写**：全局单写连接（`SetMaxOpenConns(1)`）+ 写操作串行化——SQLite 推荐姿势，杜绝 `SQLITE_BUSY`；
- **读**：多个只读连接并行（WAL 下读写不阻塞）；
- **容量边界**：单写者吞吐 ~1k 写/s、读近乎无限——初版推荐流/搭局/协商的写频远低于此；
- **WS 长连**：内存态，与 SQLite 无关，单实例 5w 连接能力不变。

**这与演进路径的关系**：单写多读是「单进程」的天然形态，不是 SQLite 逼出来的——PG 版（演进第 1 级）就是为了让多副本成为可能。

### 2.4 外部依赖策略（初版只有 LLM 一个真实外部依赖）

| 外部服务 | 接口 | 初版实现 |
|---|---|---|
| LLM（混元/GPT） | `llm.TimeWindowResolver` | mock（时间窗取交集、费用方案模板，延迟 1~2s）→ 联调切真实 API |
| ~~微信支付~~ | `pay.Ledger` | **初版不接支付**：纯记账（见 §3.7），无外部调用 |
| 短信验证码 | `sms.Sender` | 验证码打印到 server 日志 |
| geohash | 纯本地计算 | 无外部调用 |

### 2.5 支付初版降级：从「资金通道」降为「纯记账」

**产品立场**：搭趣的核心价值是找人 + AI 协商，不是资金通道。初版砍掉支付后，费用分摊以「协商结果 + 记账单」呈现——AA 多少、谁垫付，搭局结算页写得清清楚楚，**钱在线下自理**（微信转账/现金，用户本来就这么干）。

- 业务闭环不变：打卡 → 生成结算单 → 互评 → 信用分更新，全部照常（结算单只是账面记录，无资金划转）；
- `settle_orders` 表保留（记录费用模式与金额，`wx_order_id` 列保留但恒空）；
- 事件 `squad.settled` 照常发布（消费者从 pay 改为 credit，见 §5）；
- 接入真实支付的演进触发器：双边密度成型（同城日成局 >500 单）或用户强诉求，接入点已隔离在 `pay.Ledger` 接口后。

---

## 3. 核心模块详设（职责不变，实现归一到 SQLite）

### 3.1 搭局状态机（squad）—— 契约不变

7 状态 8 迁移与上游文档完全一致（IDLE/POOLED/NEGOTIATING/CONFIRMED/SETTLED/REVIEWED/EXPIRED，详见《技术设计文档.md》4.1）。

**乐观锁在单写者模型下天然简化**：所有写走单连接串行执行，`UPDATE squads SET status=? WHERE id=? AND status=?` 返回 0 行即状态已变——语义与 PG 版完全相同（返回值检查不省略，演进到 PG 时零改动）。

### 3.2 AI 协商引擎（negotiate）—— 不变

Intent 快照 / Round ≤ 5 / 48h 超时 / 幂等 / 降级链（mock → 混元 → GPT）全部保持。`negotiation_rounds` 表 UNIQUE 约束即幂等兜底。

### 3.3 匹配引擎（match）—— 三路召回归一到 SQL

```
score = 0.40·tag_score + 0.35·vec_score + 0.25·geo_score
```

| 召回路 | 实现（全在一个 SQLite 文件里） |
|---|---|
| tag | `user_interest` 表 JOIN 交集（建索引，毫秒级） |
| vec | `vec_cache` 表存上次 embedding 结果；冷启动期返回中性分 0.5（与 PG 版一致，行为数据积累后启用） |
| geo | 内存表 + Haversine（与 PG 版一致） |

「AI 推荐理由」LLM 生成一句话，进程内 LRU 缓存（容量 10k，TTL 24h）——与 PG 版一致。

### 3.4 WebSocket 网关（ws）—— 不变

单实例 map 管连接；信封 `{type, seq, payload, ts}`；断线按 `last_seq` 补拉（消息持久化在 `ws_outbox` 表）；心跳 30s。

### 3.5 超时驱动 —— timeouts 表 + 轮询认领

```sql
-- worker 每 30s：
SELECT * FROM timeouts
WHERE due_at <= datetime('now') AND claimed_at IS NULL
LIMIT 100;
UPDATE timeouts SET claimed_at = datetime('now') WHERE id IN (...);
```

**与 PG 版的差异**：没有 `FOR UPDATE SKIP LOCKED`（SQLite 不支持）——但单写者模型下天然无竞争，先 SELECT 后 UPDATE 就是串行安全的。语义不变：认领后处理，失败重投，死信表兜底。

### 3.6 进程内事件总线 + outbox（模式不变）

事件先写 `event_outbox`（SQLite 表）再投递；投递器 200ms 扫描未投递事件；失败×3 → `dead_letter`。这套 outbox 纪律与 PG 版完全相同，演进换 MQ 时投递器换 producer 即可。

### 3.7 pay 模块（初版）—— 纯记账实现

```go
// pay.Ledger 接口：初版唯一实现 PayLaterLedger（记账），未来接微信分账换实现类
type Ledger interface {
    CreateOrder(ctx context.Context, o SettleOrder) error        // 落 settle_orders 表
    MarkSettled(ctx context.Context, orderID string) error       // 状态机照常流转
}

type PayLaterLedger struct{ db *sql.DB }   // 不调用任何外部支付 API
```

**行为**：`squad.confirmed` 事件 → 生成结算单（CREATED）；打卡 → 状态流转（PENDING → SUCCESS）；`squad.settled` 事件 → credit 模块邀评。**全程零外部调用**，`wx_order_id` 恒空。结算单在客户端呈现为「费用明细账面记录」，金额与分摊模式来自协商方案卡。

**用户视角文案**：「费用 AA · 人均 ¥45（线下自理）」——账目清楚，钱不经平台。

---

## 4. 数据模型（SQLite 方言 DDL）

```sql
-- SQLite 特性使用说明：
--   TEXT 主键 + ULID（无 UUID 扩展依赖）
--   JSONB → TEXT 存 JSON 字符串（应用层序列化）
--   数组（rating tags）→ JSON TEXT
--   TIMESTAMPTZ → TEXT（ISO 8601，应用层统一 UTC）

CREATE TABLE users (
    id            TEXT PRIMARY KEY,           -- u_{ulid}
    phone         TEXT UNIQUE NOT NULL,
    nickname      TEXT NOT NULL,
    avatar_url    TEXT,
    city          TEXT,
    credit_score  INTEGER DEFAULT 620,
    status        TEXT DEFAULT 'ACTIVE',
    created_at    TEXT DEFAULT (datetime('now'))
);

CREATE TABLE user_interest (
    user_id    TEXT NOT NULL REFERENCES users(id),
    tag        TEXT NOT NULL,
    weight     REAL DEFAULT 1.0,
    updated_at TEXT DEFAULT (datetime('now')),
    PRIMARY KEY (user_id, tag)
);
CREATE INDEX idx_interest_tag ON user_interest(tag);   -- 标签召回主力

CREATE TABLE user_pref (
    user_id     TEXT PRIMARY KEY REFERENCES users(id),
    cost_mode   TEXT DEFAULT 'FLEXIBLE',
    time_slots  TEXT,                                   -- JSON
    role_pref   TEXT DEFAULT 'ANY'
);

CREATE TABLE squads (
    id            TEXT PRIMARY KEY,                     -- sq_{ulid}
    initiator_id  TEXT NOT NULL REFERENCES users(id),
    status        TEXT NOT NULL,                       -- 状态机
    activity      TEXT NOT NULL,
    time_window   TEXT NOT NULL,                        -- JSON
    cost_mode     TEXT,
    expire_at     TEXT,
    created_at    TEXT DEFAULT (datetime('now')),
    updated_at    TEXT DEFAULT (datetime('now'))
);
CREATE INDEX idx_squads_status ON squads(status, expire_at);

CREATE TABLE candidate_pool (
    id              TEXT PRIMARY KEY,
    user_id         TEXT NOT NULL REFERENCES users(id),
    candidate_id    TEXT NOT NULL REFERENCES users(id),
    intent_snapshot TEXT NOT NULL,                      -- JSON（上推时冻结）
    status          TEXT DEFAULT 'WAITING',
    created_at      TEXT DEFAULT (datetime('now')),
    UNIQUE (user_id, candidate_id)
);

CREATE TABLE negotiations (
    id              TEXT PRIMARY KEY,                   -- ngt_{ulid}
    squad_id        TEXT REFERENCES squads(id),
    candidate_id    TEXT NOT NULL,
    round           INTEGER DEFAULT 0,
    status          TEXT DEFAULT 'ACTIVE',
    created_at      TEXT DEFAULT (datetime('now')),
    expire_at       TEXT NOT NULL
);

CREATE TABLE negotiation_rounds (
    id             TEXT PRIMARY KEY,
    negotiation_id TEXT NOT NULL REFERENCES negotiations(id),
    round_no       INTEGER NOT NULL,
    proposal       TEXT NOT NULL,                       -- JSON（LLM 方案卡）
    feedback       TEXT,                                -- JSON（双方反馈）
    created_at     TEXT DEFAULT (datetime('now')),
    UNIQUE (negotiation_id, round_no)
);

CREATE TABLE reviews (
    id         TEXT PRIMARY KEY,
    squad_id   TEXT REFERENCES squads(id),
    reviewer   TEXT NOT NULL,
    reviewee   TEXT NOT NULL,
    rating     INTEGER CHECK (rating BETWEEN 1 AND 5),
    tags        TEXT,                                   -- JSON 数组
    comment    TEXT,
    created_at TEXT DEFAULT (datetime('now')),
    UNIQUE (squad_id, reviewer)
);

CREATE TABLE settle_orders (
    id          TEXT PRIMARY KEY,                       -- so_{ulid}
    squad_id    TEXT REFERENCES squads(id),
    mode        TEXT NOT NULL,                          -- AA/ROTATE/TREAT
    amount      INTEGER NOT NULL,                       -- 分
    wx_order_id TEXT,
    status      TEXT DEFAULT 'CREATED',
    created_at  TEXT DEFAULT (datetime('now'))
);

-- ============ 轻量化承接表（与 PG 版同构，类型方言化） ============
CREATE TABLE timeouts (
    id          TEXT PRIMARY KEY,
    due_at      TEXT NOT NULL,
    kind        TEXT NOT NULL,                          -- NEGOTIATION_EXPIRE / CHECKIN_GRACE / REVIEW_DEFAULT
    payload     TEXT NOT NULL,                          -- JSON
    claimed_at  TEXT
);
CREATE INDEX idx_timeouts_due ON timeouts(due_at) WHERE claimed_at IS NULL;

CREATE TABLE event_outbox (
    id         TEXT PRIMARY KEY,                        -- event_id（幂等键）
    topic      TEXT NOT NULL,
    payload    TEXT NOT NULL,                           -- JSON
    created_at TEXT DEFAULT (datetime('now')),
    delivered  INTEGER DEFAULT 0
);
CREATE INDEX idx_outbox_undelivered ON event_outbox(delivered, created_at);

CREATE TABLE ws_outbox (
    id         TEXT PRIMARY KEY,
    user_id    TEXT NOT NULL,
    seq        INTEGER NOT NULL,
    type       TEXT NOT NULL,
    payload    TEXT NOT NULL,                            -- JSON
    created_at TEXT DEFAULT (datetime('now'))
);
CREATE INDEX idx_ws_outbox_user_seq ON ws_outbox(user_id, seq);

CREATE TABLE dead_letter (
    id          TEXT PRIMARY KEY,
    topic       TEXT NOT NULL,
    payload     TEXT NOT NULL,
    err         TEXT,
    created_at  TEXT DEFAULT (datetime('now'))
);
```

**位置数据零落库原则不变**：实时坐标内存表（TTL 15min），匹配即取即算。

---

## 5. 事件契约（8 主题不变）

主题、载荷、消费者与前一版完全一致，进程内总线实现：

| 主题 | 生产者 | 消费者 |
|---|---|---|
| `pool.joined` | squad | negotiate |
| `negotiation.proposal` | negotiate | ws |
| `negotiation.expired` | negotiate | squad |
| `squad.confirmed` | squad | im、pay（记账单生成） |
| `squad.settled` | pay | credit（邀评；初版无资金划转） |
| `credit.updated` | credit | user |
| `squad.status_changed` | squad | ws |
| `risk.flagged` | risk | match、squad |

### 5.1 事件可靠性（outbox 模式，纪律不变）

事件先落 `event_outbox` 再投递；投递器 200ms 扫描；失败×3 → `dead_letter`；消费方按 `event_id` 幂等。崩溃重启后未投递事件自动补发。

```
模块事务 ──写──▶ event_outbox ──投递器(200ms)──▶ 进程内总线 ──▶ 订阅模块
                     │                              │
                     └── 重启后未投递自动补发          └── 失败×3 → dead_letter
```

---

## 6. API 规范（不变）

11 端点、响应信封 `{code, msg, data, req_id}`、错误码段（1xxx~5xxx）、写操作 `Idempotency-Key` 头——与客户端文档完全一致，一字未改。限流用进程内令牌桶（按 IP+UID，默认 100 req/s）。

---

## 7. 测试与可观测

| 项 | 实现 |
|---|---|
| 单元 | 状态机迁移矩阵全排列测试（不变） |
| 集成 | **无需 testcontainers**——SQLite 是文件，`t.TempDir()` 建临时库即测，毫秒级 |
| 端到端 | `make seed` + 脚本全链路（不变） |
| 指标 | /metrics 自带（不变） |
| 审计 | outbox/dead_letter 表即流水 |

**测试提速红利**：集成测试从「起容器等 10 秒」变成「建临时文件毫秒级」——CI 单测套件可以全量跑集成路径。

---

## 8. 中间件替代速查表

| 全功能版组件 | 本版替代 | 演进触发器 |
|---|---|---|
| PostgreSQL | SQLite（WAL 单写多读） | 写 QPS >500 或多副本需求 |
| Redis 缓存 | 进程内 LRU | 命中率 <70% 或多副本 |
| Redis GEO | 内存表 + Haversine | geo P95 >50ms |
| Redis 延迟队列 | timeouts 表 + 轮询认领 | 扫描拖慢主库 |
| ES 标签召回 | user_interest JOIN + 索引 | tag 召回 P95 >100ms |
| Milvus 向量 | vec_cache 表 + 中性分 | 向量数据 >100w 条 |
| NATS/Kafka | 进程内总线 + outbox | 多副本或跨进程消费 |
| 微信分账 API | 纯记账 PayLaterLedger | 双边密度成型（日成局 >500）或用户强诉求 |

---

## 9. 演进路径：SQLite → PG → 全功能（三级升配）

> 每级触发器明确、动作独立。**数据迁移是平滑的**：SQLite 与 PG 的表结构同构（本版 DDL 按可迁移性设计），pgloader 一条命令。

| 级 | 触发器 | 动作 | 代码改动量 |
|---|---|---|---|
| 1 | 写 QPS >500 / 需多副本 / DAU >2w | SQLite → PG（pgloader 迁数据，改 DATABASE_URL，方言适配层切换） | 小：方言层 + JSONB 转换 |
| 2 | 召回延迟超标 / 缓存命中率下降 | 上 ES/Milvus/Redis（查询接口不变，换适配层） | 中：每组件一个适配层 |
| 3 | DAU >5w / 多城 | 模块化单体 → 微服务（模块边界已按拆分预设） | 大：模块 → 服务平移 |

**关键伏笔都埋好了**：模块间只依赖 interface + 事件契约；数据按模块分表（演进到 PG 时按模块分 schema）；SQL 全部集中在 repository 层（方言差异不外溢）；outbox 模式让事件消费与存储解耦——每一项演进都是「换实现类」的量级。

---

## 10. 本地开发环境要求

| 项 | 要求 |
|---|---|
| 硬件 | 任意能跑 Go 的机器 |
| 软件 | **Go 1.22，没了**（无 Docker、无数据库、无 CGO） |
| 首次启动 | `make dev` ≈ 5s |
| 备份 | `cp data/spotpal.db backup.db`（或在线 VACUUM INTO） |

---

*本文档为《详细设计-服务端.md》SQLite 零依赖版（支付初版纯记账）；《技术设计文档.md》与《详细设计-客户端.md》同步更新。部署拓扑画布（云端终态图）作为演进目标参考。*
