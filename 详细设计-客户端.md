# 搭趣 SpotPal · Android 客户端详细设计

> **仓库** `spotpal-android` · Kotlin 2.0 + Jetpack Compose + MVI（单向数据流）
> **支持范围** Android 8.0+（minSdk 26，targetSdk 34）；iOS / 小程序不在本仓库
> **上游契约** 《技术设计文档.md》V1.0 第 6 章 API；《详细设计-服务端.md》§3.4 WS 网关、§5 事件、§6 错误码
> **配套设计稿** Ardot `723889208012364` Page 1（六屏高保真）、`724002923294341`（上推托付手势 4 帧）

---

## 1. 架构总览

### 1.1 技术选型

| 领域 | 选型 | 理由 |
|---|---|---|
| 语言 | Kotlin 2.0（含 Compose Compiler 插件） | 官方一等公民 |
| UI | Jetpack Compose + Material3 | 声明式 UI 是手势/动画密集型产品的最优解（上推托付全程自绘） |
| 架构模式 | MVI（StateFlow 单一状态源） | 手势状态机与 UI 状态天然同构，回放调试容易 |
| DI | Hilt | Compose/ViewModel 生态成熟 |
| 网络 | Retrofit2 + OkHttp4 + kotlinx.serialization | §3.1 |
| 存储 | Room + DataStore(Proto) | 候选池/消息缓存 + 轻量偏好 |
| 异步 | Coroutines + Flow（StateFlow/SharedFlow） | MVI 基础设施 |
| 导航 | Navigation-Compose | 3 Tab + 6 屏 |
| 图片 | Coil | Compose 原生支持 |
| 定位 | 高德定位 SDK（需单独同意页） | 商圈级上报由 SDK 端完成；合规认证齐全，国内安卓生态覆盖率最高 |
| 支付 | **初版无支付 SDK** | 结算页纯记账展示（与服务端 PayLaterLedger 配套，费用线下自理）；接入真实支付时按需引入 |
| 埋点 | 预留 Tracker 接口（先本地落日志） | M4 再接云端 |

### 1.2 模块化（Gradle 多模块）

```
spotpal-android/
├── app/                        # 壳工程：Navigation、Application、DI 装配
├── core/
│   ├── design/                 # 主题/颜色/字体/通用组件（设计系统落位）
│   ├── network/                 # Retrofit/OkHttp/WS/信封/错误码
│   ├── common/                  # Result 封装、协程调度器注入
│   ├── data/                    # Room + Repository 实现
│   └── model/                   # 纯数据类（DTO/领域模型），无 Android 依赖
└── feature/
    ├── discover/                # ① 发现：推荐卡流 + 上推托付手势
    ├── profile/                 # ② 搭子主页
    ├── negotiate/               # ③ AI 协商（方案卡流）
    ├── confirm/                 # ④ 搭局确认
    ├── squads/                  # ⑤ 我的搭局
    └── me/                      # ⑥ 我的
```

**依赖规则**：feature 之间禁止互相依赖（经 Navigation 深链跳转）；feature → core 单向。接口定义在 core/data，实现也在 core/data，feature 只拿 Repository——与服务端「接口定义在消费方」相反，客户端以**编译期隔离**换构建速度。

### 1.3 MVI 骨架

```kotlin
// 每个 feature 统一三件套
data class DiscoverUiState(
    val cards: List<RecommendCard> = emptyList(),
    val poolCount: Int = 0,                      // 候选池人数（实时）
    val gesture: GesturePhase = GesturePhase.Idle, // 手势状态机（§3.3）
    val loading: Boolean = false,
)

sealed interface DiscoverIntent {                 // 用户动作（含手势回调）
    data class LongPressed(val card: RecommendCard) : DiscoverIntent
    data class PushPassed(val card: RecommendCard) : DiscoverIntent
    data class Released(val card: RecommendCard, val passed: Boolean) : DiscoverIntent
    data object Refresh : DiscoverIntent
}

sealed interface DiscoverEffect {                 // 一次性副作用（toast/导航/震动）
    data class PoolToast(val count: Int) : DiscoverEffect
    data class NavigateToNegotiation(val id: String) : DiscoverEffect
}
```

ViewModel 内 `MutableStateFlow<UiState>` + `fun dispatch(intent)`，UI 侧 `collectAsStateWithLifecycle()`。

---

## 2. 核心模块详设

### 2.1 网络层（core/network）

**基础配置**

```kotlin
@Singleton
class SpotPalClient @Inject constructor(
    prefs: TokenStore,
) {
    val okHttp = OkHttpClient.Builder()
        .connectTimeout(5, SECONDS).readTimeout(15, SECONDS)
        .pingInterval(30, SECONDS)                 // WS 心跳（与服务端 3.4 对齐）
        .addInterceptor(AuthInterceptor(prefs))     // Bearer + 401 刷新重试（同步单飞）
        .addInterceptor(IdempotencyInterceptor())   // 写操作自动挂 Idempotency-Key=UUID，重试复用
        .addInterceptor(TraceInterceptor())         // X-Req-Id 透传，与服务端 traceId 贯通
        .build()
}
```

**响应信封映射**（服务端 §6 约定 `{code, msg, data, req_id}`）

```kotlin
@Serializable
data class Envelope<T>(val code: Int, val msg: String, val data: T? = null, @SerialName("req_id") val reqId: String? = null)

// 错误码段 → 领域异常（对齐服务端 §6）
// 1xxx 通用（1001 token 失效→触发登出）；2xxx 用户域；3xxx 搭局域（3002 池满→提示）
// 4xxx 协商域（4001 轮次超限）；5xxx 结算域（初版纯记账，预留段）
class BizError(val code: Int, override val message: String) : Exception(message)
```

**API 接口清单**（11 个端点与服务端一一对应）

```kotlin
interface SpotPalApi {
    @POST("v1/pool/push")            suspend fun push(@Body body: PushReq): Envelope<PoolItem>          // 幂等
    @GET("v1/pool")                   suspend fun pool(@Query("cursor") c: String?): Envelope<Page<PoolItem>>
    @DELETE("v1/pool/{candidateId}") suspend fun remove(@Path("candidateId") id: String): Envelope<Unit>
    @POST("v1/negotiation/release")  suspend fun release(): Envelope<Negotiation>
    @GET("v1/negotiation/{id}")       suspend fun negotiation(@Path("id") id: String): Envelope<Negotiation>
    @POST("v1/negotiation/{id}/feedback") suspend fun feedback(@Path("id") id: String, @Body b: FeedbackReq): Envelope<Unit>
    @POST("v1/squad/{id}/confirm")   suspend fun confirm(@Path("id") id: String): Envelope<Squad>
    @POST("v1/squad/{id}/checkin")   suspend fun checkin(@Path("id") id: String): Envelope<SettleOrder>
    @GET("v1/feed/recommend")        suspend fun recommend(@Query("cursor") c: String?, @Query("lat") lat: Double?, @Query("lng") lng: Double?): Envelope<Page<RecommendCard>>
    @GET("v1/ws/messages")           suspend fun pullAfter(@Query("after_seq") seq: Long): Envelope<Page<WsMessage>>  // 断线补拉
}
```

### 2.2 WebSocket 连接管理器（core/network/WsClient.kt）

对齐服务端 ws 网关契约：信封 `{type, seq, payload, ts}`、ACK 回传 seq、断线按 `last_seq` 补拉、心跳 30s、服务端断连前发 `going_away`。

```kotlin
@Singleton
class WsClient @Inject constructor(private val scope: CoroutineScope, private val api: SpotPalApi) {
    private val _state = MutableStateFlow<WsConn>(WsConn.Disconnected)
    val state: StateFlow<WsConn> = _state                       // UI 显示「已离线」横幅

    private val _events = MutableSharedFlow<WsMessage>(extraBufferCapacity = 64)
    val events: SharedFlow<WsMessage> = _events                 // feature 按需订阅过滤

    private var lastSeq: Long = 0                               // 启动时从 DataStore 恢复

    fun connect(token: String) = scope.launch { loop@ {        // 外层重连循环
        val socket = okHttp.newWebSocket(request(token), listener)
        // ① 消息到达：先持久化 seq → 回 ACK → 再分发
        // ② ACK 帧：{"type":"ack","ack":seq}
        // ③ 心跳：OkHttp pingInterval(30s) 底层保活；收到 going_away 主动断开不视为异常
        // ④ 重连退避：1s → 2s → 4s → ... 上限 60s，加 ±20% 抖动；前台切后台 5 分钟后主动断开
    }}

    private suspend fun resume(socket: WebSocket) {
        // 连接建立后发 {"type":"resume","last_seq":lastSeq}
        // 服务端补发缺口；若缺口过大（>500 条）降级走 REST /v1/ws/messages?after_seq= 拉取
    }
}
```

**消息分发**（5 类推送 → 各 feature 订阅）

| type | 消费方 | 动作 |
|---|---|---|
| `negotiation.proposal` | feature/negotiate | 方案卡插入消息流 + 通知栏（后台时） |
| `negotiation.confirmed` | feature/confirm | 状态刷新 + 全局 Result「成局」 |
| `squad.status` | feature/squads | 卡片状态迁移 + 倒计时重算 |
| `im.message` | feature/negotiate | 气泡流 |
| `system.notice` | 全局 | toast / 强提示 |

**可靠性红线**：seq 必须先落盘再分发（Room 单表 `ws_seq(seq PK, acked)`），进程被杀也不丢；消费端按 `event_id` 幂等。

### 2.3 上推托付手势引擎（core/design/gesture）★核心

对应设计稿 4 帧：**静置 → 长按微调 → 上推托付 → 释放锁定**。

**参数表**（与《技术设计文档.md》2.2 一致，全部收口在 GestureSpec 便于调参）

```kotlin
object GestureSpec {
    val LongPressTimeout = 600.milliseconds     // 长按弹出微调面板
    val PushThreshold = 80.dp                   // 上推过阈值即触发托付（按密度归一化）
    val TouchSlop = 12.dp                       // 位移容差：未超过视为"没动"，不取消长按计时
    val SnappedScale = 0.6f                     // 阈值过后卡片视觉缩小比例
}
```

**手势状态机**（与 UiState.gesture 同构，4 态）

```kotlin
sealed interface GesturePhase {
    data object Idle : GesturePhase                                            // ① 静置浏览
    data class Adjusting(val cardId: String) : GesturePhase                     // ② 长按面板已弹出
    data class Pushing(val dy: Float, val passed: Boolean) : GesturePhase      // ③ 上推中（含实时位移）
    data object Released : GesturePhase                                        // ④ 松手锁定
}
```

**Compose 实现（核心骨架）**

```kotlin
@Composable
fun PushableCard(
    card: RecommendCard,
    vm: DiscoverViewModel,
    modifier: Modifier = Modifier,
) {
    val offsetY = remember { Animatable(0f) }
    val scale by animateFloatAsState(if (gesture.passed) 0.6f else 1f, spring(stiffness = 400f))
    var longPressJob by remember { mutableStateOf<Job?>(null) }

    Box(modifier
        .graphicsLayer { translationY = offsetY.value; scaleX = scale; scaleY = scale }
        .pointerInput(card.id) {
            awaitEachGesture {
                val down = awaitFirstDown()
                var passed = false
                // —— 长按检测：600ms 内位移 < TouchSlop 才算长按 ——
                longPressJob = launch {
                    withTimeoutOrNull(GestureSpec.LongPressTimeout) {
                        var total = 0f
                        waitForUpOrCancellationWhile { total < touchSlopPx }  // 等待静止
                    } ?: return@launch
                    vm.dispatch(LongPressed(card))                              // 弹微调面板（②）
                }
                // —— 垂直拖拽：主循环 ——
                do {
                    val event = awaitPointerEvent()
                    val dy = event.calculateDragY(down.id)
                    if (abs(dy) > touchSlopPx) longPressJob?.cancel()          // 一动就取消长按
                    if (dy < 0) {                                              // 只认上推（负值）
                        offsetY.snapTo(offsetY.value + dy)
                        val passedNow = -offsetY.value > touchSlopToPx(GestureSpec.PushThreshold)
                        if (passedNow != passed) { passed = passedNow; haptic() } // 过阈值震动反馈
                        vm.dispatch(PushProgress(card, offsetY.value, passed))  // 更新 ③
                    }
                } while (event.changes.any { it.pressed })
                // —— 松手：过阈值→上飞；否则回弹（防误触）——
                if (passed) {
                    launch {                                                   // 卡片飞入顶部候选池
                        offsetY.animateTo(-size.height * 1.2f, tween(220, easing = FastOutSlowInEasing))
                        vm.dispatch(Released(card, passed = true))            // ④ POOLED
                        offsetY.snapTo(0f)
                    }
                } else {
                    launch {                                                   // 松手回弹
                        offsetY.animateTo(0f, spring(dampingRatio = Spring.DampingRatioMediumBouncy))
                        vm.dispatch(Released(card, passed = false))            // 取消，卡片回原位
                    }
                }
            }
        }) { CardContent(card) }
}
```

**实现要点**

| 问题 | 方案 |
|---|---|
| 长按与拖拽冲突 | 长按协程可被拖拽取消（位移 > TouchSlop）；面板弹出后接管手势消费 |
| 防误触 | 双保险：80dp 阈值 + 松手回弹（spring 阻尼中等） |
| 面板内滑块与外层手势 | 微调面板（时间滑块）在自己的 `pointerInput` 消费水平拖拽，并对外层手势加 `Modifier.semantics { invisibleToUserGesture() }` 隔离 |
| 候选池人数实时反馈 | 池计数走 WS `squad.status` + 本地乐观更新；卡片上飞终点即池入口的坐标（`onGloballyPositioned` 采样） |
| 性能 | 全部动画只动 `graphicsLayer` 属性（translationY/scale），不触发重组布局；用 `derivedStateOf` 收敛 passed 布尔 |
| 无障碍降级 | TalkBack 模式下提供替代交互：卡片菜单里显式「加入候选池」菜单项（`customActions`） |

**首次引导**：首次进入发现页覆盖 3 帧引导浮层（复用设计稿 4 帧图），`DataStore.guide_shown` 一次性标记。

### 2.4 候选池与推荐流（feature/discover）

```
DiscoverViewModel
  ├─ recommendFlow: 分页加载（cursor 式，预取下一页 50% 触发）
  ├─ poolFlow: Room 表 candidate_pool 离线可读 + 上推成功后乐观插入
  └─ 上推事务：UI 即时上飞 → Room 乐观插入 → POST /v1/pool/push
       成功：WS 回包覆盖本地
       失败（3002 池满）：回滚 Room + 卡片回弹动画 + toast「候选池已满（5 人）」
```

**位置策略**（与服务端「商圈级模糊化」对齐）：定位由高德 SDK 上报商圈级坐标（1km 网格），客户端只在推荐卡显示「福田 · 800m」相对距离；**App 本地不存储任何坐标**，定位结果只进网络请求参数。

### 2.5 本地存储（core/data）

| 存储 | 内容 | 策略 |
|---|---|---|
| Room `ws_messages` | WS 消息（seq/ack/event_id/type/payload） | 滚动 7 天清理；seq 持久化保证补拉 |
| Room `candidate_pool` | 候选池镜像 | 离线可看，联网以服务端为准（version 向量合并） |
| Room `negotiations` | 协商会话 + 轮次缓存 | 详情页秒开，进入页面再 refresh |
| DataStore | token、last_seq、guide_shown、user_pref（时间窗/费用/角色偏好） | Proto 格式 |

---

## 3. UI 层设计

### 3.1 设计系统映射（core/design）

| 设计稿（Apple 体系） | Android 落位 | 值 |
|---|---|---|
| 品牌蓝 #0066CC | `SpotPalColor.Primary` | 6600CC |
| 暗卡 #272729（AI 推荐理由/信用分卡） | `SpotPalColor.DarkCard` | 272729 |
| 1px 细边框、无阴影 | `CardDefaults.outlinedCard` + 自定义 `BorderStroke(1.dp, Line)` | 海拔 0 |
| 18px 卡片圆角 | `RoundedCornerShape(18.dp)` | 统一 Shape |
| SF Pro / PingFang | 默认 Roboto + 系统中文（厂商 MiSans/OPPO Sans）；**不打包字体**（首包体积 +5MB 不值） | `Typography` 全自定义行高对齐设计稿 |
| 搭局条组件 | `SquadChip`（时间滑块/项目/费用/角色四槽位） | 共享组件，发现页与微调面板复用 |

**单手拇指热区**：发现页所有交互控件下移——卡片 Fling 操作区占屏高 65%，池入口锚定在顶部安全区下方，顶部栏只读。

### 3.2 导航结构

```
RootNavHost
├── MainScaffold（bottomBar：发现 | 我的搭局 | 我的）     ← 3 Tab
│   ├── discover          ① 发现（含上推托付手势）
│   │   └── profile/{uid} ② 搭子主页
│   ├── squads            ⑤ 我的搭局
│   │   ├── negotiate/{id} ③ AI 协商（也可从 discover 深链进入）
│   │   │   └── confirm/{id} ④ 搭局确认
│   │   └── review/{id}   互评页
│   └── me                ⑥ 我的
└── auth（登录，未登录时为起点）
```

深链统一 `spotpal://squad/{id}`（WS 通知点击跳转）。

### 3.3 关键页面要点

- **③ AI 协商页**：消息流 = LazyColumn（方案卡/费用选项/暗色汇总卡三种气泡）；方案卡提供「确认 / 微调」双 CTA，微调走 `feedback` 接口；协商进度条按 round/5 渲染；48h 倒计时本地 `CountdownTimer` + WS 状态兜底。
- **④ 搭局确认页**：费用明细三模式切换（AA/轮流/一人请）即时联动结算金额预览；「确认锁单」走 `confirm` 接口。**初版无支付拉起**：结算金额以记账形式展示，文案「费用 AA · 人均 ¥45（线下自理）」，`checkin` 打卡后状态照常流转（PENDING → SUCCESS，服务端纯记账）。
- **⑤ 我的搭局**：进行中卡倒计时（EXP（超时回流的 EXPIRED）态置灰+回流提示）；状态迁移全部由 WS `squad.status` 驱动，REST 仅兜底。

---

## 4. 安全与合规（Android 侧）

| 项 | 实现 |
|---|---|
| 位置权限 | 单独同意页（个保法）：请求前自解释 → `ACCESS_COARSE_LOCATION`（商圈级够用，**不请求精确定位**）→ 拒绝后城市维度匹配可用 |
| 隐私政策 | 首启弹窗 + 工信部「隐私摘要」二次确认；SDK 清单页展示（高德定位/统计） |
| 传输 | 全站 HTTPS + 证书锁定（OkHttp CertificatePinner，仅锁主域） |
| 本地 | Room 不存位置/通讯录；token 存 EncryptedSharedPreferences（DataStore 迁移中，M4 前） |
| 支付 | **初版无支付 SDK、无资金信息**——结算页纯记账展示，零资金合规面 |
| 混淆 | R8 全量 + 规则白名单（serialization/高德 SDK）；发布包禁用日志 |

---

## 5. 测试策略

| 层 | 工具 | 覆盖重点 |
|---|---|---|
| 手势单测 | Robolectric + `MockMotionEvent` 注入 pointerInput | 4 态全迁移矩阵：长按中断/阈值边缘（79dp vs 80dp）/回弹/上飞；**阈值边界用参数化测试扫 70~90dp** |
| ViewModel | Turbine + kotlinx-coroutines-test | WS 推送 → UiState 映射；断线补拉幂等 |
| 快照 | Paparazzi（Compose 截图测试） | 六屏主状态像素级比对，防 UI 回归 |
| E2E 手工 | 联调脚本（服务端 `make seed` 数据 + 预置账号矩阵） | 上推→释放→协商→确认→结算全链路，断线 20 次重连 |

---

## 6. 构建与发布

- **版本**：semVer + 构建号；`versionCode` 单调递增。
- **变体**：`debug`（连本地 `http://10.0.2.2:8080`，MacBook 模拟器直连宿主机 Compose 栈）／`staging`（云测试环境）／`release`。
- **CI**（GitHub Actions）：`assemble` + 单测 + Paparazzi 快照 diff → 内测分发（蒲公英/fir）→ 灰度。
- **包体目标**：首包 ≤ 25MB（不打包字体/地图全量离线包，定位 SDK 按需 ABI 分发 ` splits.abi`）。

---

## 7. 与服务端联调对照表

| 客户端动作 | 服务端端点/事件 | 联调验证点 |
|---|---|---|
| 上推过阈值松手 | `POST /v1/pool/push`（幂等键） | 弱网下重复请求返回同一记录（3002 池满除外） |
| 释放委托 | `POST /v1/negotiation/release` → 事件 `pool.joined` | WS 收到 `negotiation.proposal` 的端到端时延（mock LLM ≈2s） |
| 断网恢复 | `resume last_seq` + REST 补拉 | 飞行模式开关 10 次，消息零丢失零重复 |
| 确认成局 | `POST /v1/squad/{id}/confirm` → `squad.confirmed` | 双端同时确认的竞态（后端乐观锁兜底） |
| 打卡结算 | `POST /v1/squad/{id}/checkin` → `squad.settled` | 结算单状态流转（纯记账，无支付拉起）；互评邀达 |

**本地联调启动**：服务端 `make dev`（单二进制，任意目录可跑），Android Studio 选 debug 变体——模拟器 `10.0.2.2` 直达宿主机，真机连同一 Wi-Fi 用局域网 IP，`BuildConfig` 注入。

---

*本文档与《详细设计-服务端.md》配套构成详细设计全集；部署拓扑见 Ardot「搭趣 SpotPal-部署拓扑」画布。*
